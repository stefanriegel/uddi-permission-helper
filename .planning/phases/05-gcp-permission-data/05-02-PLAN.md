---
phase: 05-gcp-permission-data
plan: 02
type: tdd
wave: 2
depends_on: [05-01]
files_modified: [js/data/gcp.js, tests/gcp-generators.test.js]
autonomous: true
requirements: [GCP-01, GCP-02, GCP-03, GCP-04, GCP-05, GCP-06, GCP-07, GCP-08, GCP-09]

must_haves:
  truths:
    - "getGcpRoles deduplicates predefined roles across multiple features"
    - "getGcpCustomPermissions merges and deduplicates custom permissions from selected features"
    - "Cloud Forwarding inbound + outbound combined produces exactly 21 unique permissions (GCP-07)"
    - "generateGcpPolicy outputs gcloud commands for role bindings and custom role creation"
    - "generateGcpTerraform outputs google_project_iam_member and google_project_iam_custom_role blocks"
    - "generateGcpGuide outputs numbered setup steps from service account creation through Infoblox Portal config"
  artifacts:
    - path: "js/data/gcp.js"
      provides: "Five generator function exports"
      exports: ["GCP_FEATURES", "getGcpRoles", "getGcpCustomPermissions", "generateGcpPolicy", "generateGcpTerraform", "generateGcpGuide"]
    - path: "tests/gcp-generators.test.js"
      provides: "Unit tests for all 5 generator functions"
      min_lines: 80
  key_links:
    - from: "tests/gcp-generators.test.js"
      to: "js/data/gcp.js"
      via: "import of all exported functions"
      pattern: "import.*from.*gcp\\.js"
    - from: "js/data/gcp.js"
      to: "Phase 7 output engine"
      via: "generator function exports"
      pattern: "export function generate"
---

<objective>
Add five generator functions to gcp.js (getGcpRoles, getGcpCustomPermissions, generateGcpPolicy, generateGcpTerraform, generateGcpGuide) with TDD and full deduplication logic.

Purpose: Completes the GCP data module so the output engine (Phase 7) can call generator functions with selected feature IDs and get formatted output.
Output: Updated js/data/gcp.js with exports + tests/gcp-generators.test.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gcp-permission-data/05-01-SUMMARY.md
@js/data/gcp.js
@js/data/aws.js
@js/data/azure.js
@tests/azure-generators.test.js
</context>

<interfaces>
<!-- Generator function signatures must mirror AWS/Azure pattern -->
From js/data/aws.js:
```javascript
export function getAwsActions(selectedFeatureIds) → string[]
export function generateAwsPolicy(selectedFeatureIds) → string
export function generateAwsTerraform(selectedFeatureIds) → string
export function generateAwsGuide(selectedFeatureIds) → string
```

From js/data/azure.js:
```javascript
export function getAzureRoles(selectedFeatureIds) → Array<{name, builtIn, scope}>
export function generateAzurePolicy(selectedFeatureIds) → string
export function generateAzureTerraform(selectedFeatureIds) → string
export function generateAzureGuide(selectedFeatureIds) → string
```

GCP needs TWO getter functions (hybrid model):
```javascript
export function getGcpRoles(selectedFeatureIds) → Array<{role, scope}>  // deduplicated predefined roles
export function getGcpCustomPermissions(selectedFeatureIds) → string[]  // deduplicated custom permissions
export function generateGcpPolicy(selectedFeatureIds) → string          // gcloud CLI commands
export function generateGcpTerraform(selectedFeatureIds) → string       // google provider HCL
export function generateGcpGuide(selectedFeatureIds) → string           // numbered steps
```
</interfaces>

<feature>
  <name>GCP Generator Functions</name>
  <files>js/data/gcp.js, tests/gcp-generators.test.js</files>
  <behavior>
    getGcpRoles:
    - Empty input returns empty array
    - assetDiscovery returns [{role: 'roles/compute.viewer', scope: 'project'}, {role: 'roles/compute.networkViewer', scope: 'project'}]
    - dnsReadOnly returns [{role: 'roles/dns.reader', scope: 'project'}]
    - cloudForwardingInbound returns empty array (custom permissions only)
    - assetDiscovery + dnsReadOnly returns 3 roles (no overlap to dedup)
    - multiProjectOrg returns 2 roles at org/folder scope
    - Selecting all features deduplicates roles by role name

    getGcpCustomPermissions:
    - Empty input returns empty array
    - storageBuckets returns exactly 2 permissions sorted
    - cloudForwardingInbound returns exactly 10 permissions sorted
    - cloudForwardingOutbound returns exactly 15 permissions sorted
    - cloudForwardingInbound + cloudForwardingOutbound combined returns exactly 21 unique permissions (GCP-07: overlap is dns.projects.get, compute.networks.get, compute.networks.list = 3 shared, so 10+15-3-1=21... verify: inbound has 10, outbound has 15, shared are dns.projects.get, compute.networks.get, compute.networks.list = 3 overlap, so 10+15-3=22. But spec says 21. Executor must count precisely from actual data.)
    - assetDiscovery returns exactly 25 permissions
    - internalRanges returns exactly 13 permissions

    generateGcpPolicy:
    - Returns gcloud commands: `gcloud projects add-iam-policy-binding` for predefined roles
    - Returns `gcloud iam roles create` command with --permissions flag for custom permissions
    - Empty input returns empty string
    - multiProjectOrg includes gcloud organizations and folders commands

    generateGcpTerraform:
    - Returns google_project_iam_member blocks for predefined roles
    - Returns google_project_iam_custom_role + google_project_iam_member for custom roles
    - Empty input returns empty string

    generateGcpGuide:
    - Returns numbered steps starting with service account creation
    - Includes predefined role binding steps
    - Includes custom role creation steps when custom permissions present
    - Ends with Infoblox Portal configuration step
    - multiProjectOrg includes org-level and folder-level instructions
  </behavior>
  <implementation>
    TDD RED-GREEN-REFACTOR:

    RED: Create tests/gcp-generators.test.js with node:test. Import GCP_FEATURES, getGcpRoles, getGcpCustomPermissions, generateGcpPolicy, generateGcpTerraform, generateGcpGuide from js/data/gcp.js. Write at minimum 20 tests covering all behaviors above. Tests MUST fail because functions do not exist yet.

    GREEN: Add the 5 exported functions to js/data/gcp.js:

    1. getGcpRoles(selectedFeatureIds): Collect predefinedRoles from selected features, deduplicate by role string using a Map, return array of {role, scope} objects.

    2. getGcpCustomPermissions(selectedFeatureIds): Collect customPermissions from selected features, deduplicate using Set, return sorted array of permission strings.

    3. generateGcpPolicy(selectedFeatureIds): Build gcloud CLI output.
       - For predefined roles: `gcloud projects add-iam-policy-binding PROJECT_ID --member="serviceAccount:SA_EMAIL" --role="ROLE"`
       - For custom permissions: `gcloud iam roles create infoblox_uddi_custom --project=PROJECT_ID --title="Infoblox UDDI Custom Role" --permissions="perm1,perm2,..."` then `gcloud projects add-iam-policy-binding PROJECT_ID --member="serviceAccount:SA_EMAIL" --role="projects/PROJECT_ID/roles/infoblox_uddi_custom"`
       - For multiProjectOrg: Include org-level and folder-level gcloud commands

    4. generateGcpTerraform(selectedFeatureIds): Build HCL output.
       - For predefined roles: `google_project_iam_member` resource blocks
       - For custom permissions: `google_project_iam_custom_role` resource + `google_project_iam_member` binding
       - For multiProjectOrg: `google_organization_iam_member` and `google_folder_iam_member` blocks

    5. generateGcpGuide(selectedFeatureIds): Build numbered step-by-step text.
       - Step 1: Create service account in GCP Console
       - Steps for predefined role binding
       - Steps for custom role creation and binding
       - Steps for org/folder level bindings if multiProjectOrg
       - Final step: Configure service account key in Infoblox Portal

    REFACTOR: Clean up if needed. All tests must pass.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD - Generator functions for GCP data module</name>
  <files>js/data/gcp.js, tests/gcp-generators.test.js</files>
  <behavior>
    - getGcpRoles([]) returns []
    - getGcpRoles(['assetDiscovery']) returns 2 roles (compute.viewer + compute.networkViewer)
    - getGcpRoles(['dnsReadOnly']) returns 1 role (dns.reader)
    - getGcpRoles(['cloudForwardingInbound']) returns [] (custom perms only, no predefined roles)
    - getGcpRoles deduplicates when same role appears in multiple features
    - getGcpCustomPermissions([]) returns []
    - getGcpCustomPermissions(['storageBuckets']) returns exactly 2 permissions
    - getGcpCustomPermissions(['cloudForwardingInbound']) returns exactly 10 permissions
    - getGcpCustomPermissions(['cloudForwardingOutbound']) returns exactly 15 permissions
    - getGcpCustomPermissions(['cloudForwardingInbound', 'cloudForwardingOutbound']) returns exactly 21 unique permissions (GCP-07)
    - getGcpCustomPermissions(['assetDiscovery']) returns exactly 25 permissions
    - getGcpCustomPermissions(['internalRanges']) returns exactly 13 permissions
    - generateGcpPolicy with predefined role feature includes 'gcloud projects add-iam-policy-binding'
    - generateGcpPolicy with custom permissions feature includes 'gcloud iam roles create'
    - generateGcpPolicy([]) returns empty string
    - generateGcpTerraform with predefined role includes 'google_project_iam_member'
    - generateGcpTerraform with custom perms includes 'google_project_iam_custom_role'
    - generateGcpTerraform([]) returns empty string
    - generateGcpGuide includes 'service account' step
    - generateGcpGuide includes 'Infoblox Portal' step
    - generateGcpGuide([]) returns empty string
    - generateGcpGuide(['multiProjectOrg']) includes organization-level instructions
  </behavior>
  <action>
    RED: Create tests/gcp-generators.test.js importing from js/data/gcp.js. Use node:test describe/it blocks. Write 20+ tests matching the behavior list above. Commit failing tests.

    GREEN: Add to js/data/gcp.js:
    - getGcpRoles: Iterate selected features, collect predefinedRoles, deduplicate by role string using Map, return [{role, scope}] array.
    - getGcpCustomPermissions: Iterate selected features, collect customPermissions, deduplicate using Set, sort alphabetically, return string[].
    - generateGcpPolicy: Build gcloud CLI commands string. Predefined roles get `gcloud projects add-iam-policy-binding`. Custom permissions get `gcloud iam roles create` + binding. MultiProjectOrg gets org/folder commands.
    - generateGcpTerraform: Build HCL string. Predefined roles → google_project_iam_member. Custom permissions → google_project_iam_custom_role + google_project_iam_member. MultiProjectOrg → google_organization_iam_member + google_folder_iam_member.
    - generateGcpGuide: Build numbered steps. Service account creation → role bindings → custom role creation → org bindings if applicable → Infoblox Portal config.

    Commit passing implementation.
  </action>
  <verify>
    <automated>node --test tests/gcp-generators.test.js</automated>
  </verify>
  <acceptance_criteria>
- All 20+ tests pass with node --test
- getGcpRoles deduplicates: selecting assetDiscovery + dnsReadOnly returns 3 unique roles
- getGcpCustomPermissions(['cloudForwardingInbound', 'cloudForwardingOutbound']).length === 21 (GCP-07)
- getGcpCustomPermissions(['assetDiscovery']).length === 25 (GCP-01)
- getGcpCustomPermissions(['internalRanges']).length === 13 (GCP-08)
- getGcpCustomPermissions(['storageBuckets']).length === 2 (GCP-02)
- generateGcpPolicy output contains 'gcloud' commands
- generateGcpTerraform output contains 'google_project_iam' resources
- generateGcpGuide output contains numbered steps
  </acceptance_criteria>
  <done>Five generator functions exported from gcp.js, all tests pass, permission deduplication verified for Cloud Forwarding combined (21), function signatures match AWS/Azure pattern for Phase 7 integration</done>
</task>

</tasks>

<verification>
- node --test tests/gcp-generators.test.js passes all tests
- node -e "import('./js/data/gcp.js').then(m => { console.log('Exports:', Object.keys(m).join(', ')); const roles = m.getGcpRoles(['assetDiscovery', 'dnsReadOnly']); console.log('Roles count:', roles.length); const perms = m.getGcpCustomPermissions(['cloudForwardingInbound', 'cloudForwardingOutbound']); console.log('CF combined:', perms.length); })"
- Exports include: GCP_FEATURES, getGcpRoles, getGcpCustomPermissions, generateGcpPolicy, generateGcpTerraform, generateGcpGuide
</verification>

<success_criteria>
Five generator functions work correctly with deduplication. Cloud Forwarding inbound+outbound produces exactly 21 combined permissions. All function signatures are consistent with AWS/Azure modules for Phase 7 integration.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gcp-permission-data/05-02-SUMMARY.md`
</output>
