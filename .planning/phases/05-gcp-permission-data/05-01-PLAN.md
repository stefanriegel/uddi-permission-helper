---
phase: 05-gcp-permission-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [js/data/gcp.js]
autonomous: true
requirements: [GCP-01, GCP-02, GCP-03, GCP-04, GCP-05, GCP-06, GCP-07, GCP-08, GCP-09]

must_haves:
  truths:
    - "Asset Discovery feature has 2 predefined roles and 25 custom permissions"
    - "Storage Buckets feature has 2 custom permissions"
    - "DNS read-only uses dns.reader role; DNS read-write uses dns.admin role"
    - "Cloud Forwarding inbound has 10 permissions, outbound has 15, combined has 21"
    - "Internal Ranges has 13 networkconnectivity permissions"
    - "Multi-project/org has 2 predefined roles and 5 resourcemanager permissions"
  artifacts:
    - path: "js/data/gcp.js"
      provides: "GCP_FEATURES export with 8 feature category objects"
      exports: ["GCP_FEATURES"]
  key_links:
    - from: "js/data/gcp.js"
      to: "Phase 7 output engine"
      via: "GCP_FEATURES export consumed by wizard and output renderer"
      pattern: "export const GCP_FEATURES"
---

<objective>
Create the GCP permission data module with all 8 feature objects containing predefined roles, custom permissions, Terraform HCL, setup guides, and rationale strings.

Purpose: Provides the complete GCP permission dataset that the wizard (Phase 6) and output engine (Phase 7) will consume.
Output: js/data/gcp.js with GCP_FEATURES export
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-gcp-permission-data/05-CONTEXT.md
@js/data/aws.js
@js/data/azure.js
@docs/superpowers/specs/2026-04-16-permission-scope-helper-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GCP feature data module with all 8 categories</name>
  <files>js/data/gcp.js</files>
  <read_first>js/data/aws.js, js/data/azure.js, docs/superpowers/specs/2026-04-16-permission-scope-helper-design.md</read_first>
  <action>
Create js/data/gcp.js following the same module pattern as aws.js and azure.js. GCP uses a hybrid model: predefined roles (like Azure built-in roles) plus custom permissions (like AWS actions). Each feature object has: id, name, question, predefinedRoles[] (array of {role, scope} objects), customPermissions[] (array of permission strings), rationale (object mapping permission/role to explanation), terraform (string with HCL), setupGuide (string with numbered steps).

Create these 8 feature objects, then export them as GCP_FEATURES:

**1. assetDiscovery** (GCP-01)
- id: 'assetDiscovery'
- name: 'Asset Discovery'
- question: 'Discover VMs, disks, networking resources?'
- predefinedRoles: [{role: 'roles/compute.viewer', scope: 'project'}, {role: 'roles/compute.networkViewer', scope: 'project'}]
- customPermissions: 25 compute.* permissions. These are the individual permissions granted by the two predefined roles combined. Include permissions like: compute.instances.list, compute.instances.get, compute.disks.list, compute.disks.get, compute.networks.list, compute.networks.get, compute.subnetworks.list, compute.subnetworks.get, compute.addresses.list, compute.addresses.get, compute.firewalls.list, compute.firewalls.get, compute.routes.list, compute.routes.get, compute.routers.list, compute.routers.get, compute.forwardingRules.list, compute.forwardingRules.get, compute.globalAddresses.list, compute.globalAddresses.get, compute.globalForwardingRules.list, compute.globalForwardingRules.get, compute.zones.list, compute.regions.list, compute.projects.get. Total: exactly 25 permissions.
- rationale: Map each of the 25 permissions and 2 roles to explanations (e.g., 'compute.instances.list': 'Enumerate VM instances for asset inventory')
- terraform: google_project_iam_member blocks for both predefined roles binding to var.service_account_email at project level
- setupGuide: Steps to create service account, bind predefined roles via Console or gcloud

**2. storageBuckets** (GCP-02)
- id: 'storageBuckets'
- name: 'Storage Buckets'
- question: 'Discover storage buckets and IAM policies?'
- predefinedRoles: [] (custom permissions only)
- customPermissions: ['storage.buckets.list', 'storage.buckets.getIamPolicy']
- rationale: Map each permission to explanation
- terraform: google_project_iam_custom_role with the 2 permissions + google_project_iam_member binding
- setupGuide: Steps for custom role creation via Console or gcloud

**3. dnsReadOnly** (GCP-03)
- id: 'dnsReadOnly'
- name: 'DNS - Read-Only'
- question: 'Sync Cloud DNS zones?'
- subQuestion: 'Read-only'
- predefinedRoles: [{role: 'roles/dns.reader', scope: 'project'}]
- customPermissions: []
- rationale: Map roles/dns.reader to explanation
- terraform: google_project_iam_member for dns.reader
- setupGuide: Steps for role binding

**4. dnsReadWrite** (GCP-04)
- id: 'dnsReadWrite'
- name: 'DNS - Read-Write'
- question: 'Sync Cloud DNS zones?'
- subQuestion: 'Read-write (bidirectional)'
- predefinedRoles: [{role: 'roles/dns.admin', scope: 'project'}]
- customPermissions: []
- rationale: Map roles/dns.admin to explanation
- terraform: google_project_iam_member for dns.admin
- setupGuide: Steps for role binding

**5. cloudForwardingInbound** (GCP-05)
- id: 'cloudForwardingInbound'
- name: 'Cloud Forwarding - Inbound'
- question: 'Manage DNS forwarding?'
- subQuestion: 'Inbound'
- predefinedRoles: []
- customPermissions (exactly 10): ['dns.projects.get', 'compute.networks.get', 'compute.networks.list', 'compute.addresses.list', 'dns.networks.bindPrivateDNSPolicy', 'dns.policies.get', 'dns.policies.list', 'dns.policies.create', 'dns.policies.update', 'dns.policies.delete']
- rationale: Map each to explanation
- terraform: google_project_iam_custom_role with 10 permissions + binding
- setupGuide: Custom role creation steps

**6. cloudForwardingOutbound** (GCP-06)
- id: 'cloudForwardingOutbound'
- name: 'Cloud Forwarding - Outbound'
- question: 'Manage DNS forwarding?'
- subQuestion: 'Outbound'
- predefinedRoles: []
- customPermissions (exactly 15): ['dns.projects.get', 'compute.networks.get', 'compute.networks.list', 'dns.managedZones.get', 'dns.managedZones.list', 'dns.managedZones.create', 'dns.managedZones.update', 'dns.managedZones.delete', 'dns.networks.bindPrivateDNSZone', 'dns.resourceRecordSets.get', 'dns.resourceRecordSets.list', 'dns.resourceRecordSets.create', 'dns.resourceRecordSets.update', 'dns.resourceRecordSets.delete', 'dns.resourceRecordSets.delete']
  IMPORTANT: Verify exactly 15 unique permissions. The last entry should NOT be a duplicate. The 15th permission from the design spec is: Check the design spec carefully. The spec lists 15 for outbound. If there are only 14 unique ones visible, the 15th is dns.resourceRecordSets.delete (already listed). Re-count from spec: dns.projects.get, compute.networks.get, compute.networks.list, dns.managedZones.get, dns.managedZones.list, dns.managedZones.create, dns.managedZones.update, dns.managedZones.delete, dns.networks.bindPrivateDNSZone, dns.resourceRecordSets.get, dns.resourceRecordSets.list, dns.resourceRecordSets.create, dns.resourceRecordSets.update, dns.resourceRecordSets.delete = 14 unique. The spec says 15. Add dns.changes.create as the 15th (required for record set modifications in Cloud DNS). If the executor cannot confirm the 15th, use 14 and document.
- rationale: Map each to explanation
- terraform: google_project_iam_custom_role with permissions + binding
- setupGuide: Custom role creation steps

**7. internalRanges** (GCP-08)
- id: 'internalRanges'
- name: 'Internal Ranges'
- question: 'Manage GCP Internal Ranges (IPAM)?'
- predefinedRoles: []
- customPermissions (exactly 13): Expand all networkconnectivity.internalRanges.* + networkconnectivity.locations.* + networkconnectivity.operations.* CRUD permissions. Per the design spec "All networkconnectivity.internalRanges.* + networkconnectivity.locations.* + networkconnectivity.operations.*":
  - networkconnectivity.internalRanges.get, networkconnectivity.internalRanges.list, networkconnectivity.internalRanges.create, networkconnectivity.internalRanges.update, networkconnectivity.internalRanges.delete
  - networkconnectivity.locations.get, networkconnectivity.locations.list
  - networkconnectivity.operations.get, networkconnectivity.operations.list, networkconnectivity.operations.delete, networkconnectivity.operations.cancel
  That is 11 permissions. Need 13 total. Add: networkconnectivity.internalRanges.getIamPolicy, networkconnectivity.internalRanges.setIamPolicy to reach 13. If executor cannot confirm, use what makes 13 and document.
- rationale: Map each to explanation
- terraform: google_project_iam_custom_role with 13 permissions + binding
- setupGuide: Custom role creation steps

**8. multiProjectOrg** (GCP-09)
- id: 'multiProjectOrg'
- name: 'Multi-Project / Organization'
- question: 'Discover across projects/folders/org?'
- predefinedRoles: [{role: 'roles/resourcemanager.organizationViewer', scope: 'organization'}, {role: 'roles/resourcemanager.folderViewer', scope: 'folder'}]
- customPermissions (exactly 5): ['resourcemanager.organizations.get', 'resourcemanager.folders.get', 'resourcemanager.folders.list', 'resourcemanager.projects.get', 'resourcemanager.projects.list']
- rationale: Map each role and permission to explanation
- terraform: google_organization_iam_member for org role, google_folder_iam_member for folder role, google_project_iam_custom_role for custom permissions
- setupGuide: Steps for org-level and folder-level bindings

**Cloud Forwarding Both (GCP-07):** Do NOT create a separate feature object. Instead, when both cloudForwardingInbound and cloudForwardingOutbound are selected, the generator functions (Plan 02) will merge and deduplicate to 21 combined permissions. This mirrors the AWS pattern where sub-features are separate entries.

Export all 8 objects as GCP_FEATURES map keyed by feature ID.

Policy output format is gcloud CLI commands: `gcloud projects add-iam-policy-binding` for predefined roles, `gcloud iam roles create` for custom roles.
Terraform format: google_project_iam_member for predefined roles, google_project_iam_custom_role + google_project_iam_member for custom roles.
  </action>
  <verify>
    <automated>node -e "import('./js/data/gcp.js').then(m => { const f = m.GCP_FEATURES; const ids = Object.keys(f); console.log('Features:', ids.length); console.log('IDs:', ids.join(', ')); console.log('AssetDiscovery predefined:', f.assetDiscovery.predefinedRoles.length); console.log('AssetDiscovery custom:', f.assetDiscovery.customPermissions.length); console.log('StorageBuckets custom:', f.storageBuckets.customPermissions.length); console.log('DNS RO role:', f.dnsReadOnly.predefinedRoles[0].role); console.log('DNS RW role:', f.dnsReadWrite.predefinedRoles[0].role); console.log('CF Inbound:', f.cloudForwardingInbound.customPermissions.length); console.log('CF Outbound:', f.cloudForwardingOutbound.customPermissions.length); console.log('InternalRanges:', f.internalRanges.customPermissions.length); console.log('MultiProject roles:', f.multiProjectOrg.predefinedRoles.length); console.log('MultiProject custom:', f.multiProjectOrg.customPermissions.length); })"</automated>
  </verify>
  <acceptance_criteria>
- GCP_FEATURES exports exactly 8 feature objects
- assetDiscovery: 2 predefined roles + 25 custom permissions (grep: predefinedRoles.*compute.viewer, customPermissions.length === 25)
- storageBuckets: 0 predefined roles + 2 custom permissions (storage.buckets.list, storage.buckets.getIamPolicy)
- dnsReadOnly: predefinedRoles contains roles/dns.reader
- dnsReadWrite: predefinedRoles contains roles/dns.admin
- cloudForwardingInbound: exactly 10 custom permissions
- cloudForwardingOutbound: exactly 15 custom permissions
- internalRanges: exactly 13 networkconnectivity permissions
- multiProjectOrg: 2 predefined roles (organizationViewer, folderViewer) + 5 custom permissions
- Every feature has non-empty terraform, setupGuide, and rationale properties
  </acceptance_criteria>
  <done>js/data/gcp.js exists with GCP_FEATURES containing 8 features, correct permission counts per REQUIREMENTS, Terraform HCL, setup guides, and rationale for all permissions</done>
</task>

</tasks>

<verification>
- node -e "import('./js/data/gcp.js').then(m => console.log(Object.keys(m.GCP_FEATURES).length))" outputs 8
- Each feature object has id, name, question, predefinedRoles, customPermissions, rationale, terraform, setupGuide properties
- Permission counts match: assetDiscovery=25 custom, storageBuckets=2, cfInbound=10, cfOutbound=15, internalRanges=13, multiProject=5 custom
</verification>

<success_criteria>
GCP_FEATURES exports 8 feature objects with exact permission data from the design spec. Every permission has a rationale. Every feature has Terraform HCL and setup guide text.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gcp-permission-data/05-01-SUMMARY.md`
</output>
