---
phase: 01-html-shell-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
  - assets/infoblox-logo.svg
  - index.html
autonomous: false
requirements:
  - DSN-01
  - DSN-02
  - DSN-03
  - DSN-04

must_haves:
  truths:
    - "Page loads with Infoblox Navy (#002b49) header bar containing white logo and product name"
    - "Three provider cards display with AWS orange (#ff9900), Azure blue (#0078d4), and GCP blue (#4285f4) top borders"
    - "Output panel uses Infoblox Dark (#0a1628) background with tab bar, action buttons, and badge placeholder"
    - "Layout stacks vertically on viewports narrower than 900px"
    - "All interactive elements have visible focus rings"
  artifacts:
    - path: "css/styles.css"
      provides: "Complete design token system and component styles"
      contains: "--color-primary: #002b49"
    - path: "index.html"
      provides: "Semantic HTML shell with all Phase 1 components"
      contains: "UDDI Permission Scope Helper"
    - path: "assets/infoblox-logo.svg"
      provides: "White Infoblox wordmark logo for header"
      contains: "svg"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "link rel=stylesheet"
      pattern: 'href="css/styles.css"'
    - from: "index.html"
      to: "assets/infoblox-logo.svg"
      via: "img src or inline SVG reference"
      pattern: "infoblox-logo"
---

<objective>
Build the complete Infoblox-branded HTML/CSS shell for the UDDI Permission Scope Helper. This delivers all four Phase 1 requirements: branding, provider cards, dark output panel, and responsive layout.

Purpose: Establishes the visual foundation that all subsequent phases wire JavaScript behavior into.
Output: Three files — `css/styles.css` (design tokens + component styles), `assets/infoblox-logo.svg` (white wordmark), `index.html` (semantic HTML shell).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-html-shell-branding/01-CONTEXT.md
@.planning/phases/01-html-shell-branding/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSS design tokens and component styles</name>
  <files>css/styles.css</files>
  <read_first>
    .planning/phases/01-html-shell-branding/01-UI-SPEC.md
    /Users/mustermann/Documents/coding/UDDI-GO-Token-Calculator/frontend/src/styles/theme.css
  </read_first>
  <action>
Create `css/styles.css` with the complete design system. The file has three sections:

**Section 1 — CSS Custom Properties in `:root`**
Copy the exact `:root` block from UI-SPEC (lines 270-323) including all spacing, typography, border-radius, brand colors, provider colors, and output panel tokens. These are the canonical values — do not modify them.

Additionally add a shadow token aligned with the Token Calculator's navy-tinted shadow approach:
```
--shadow-card: 0 1px 3px rgba(0, 43, 73, 0.06);
--shadow-card-hover: 0 2px 8px rgba(0, 43, 73, 0.08);
```

**Section 2 — Base Reset and Typography**
- Box-sizing border-box reset (`*, *::before, *::after`)
- Body: margin 0, background `var(--color-surface-alt)`, color `var(--color-text)`, font-family `var(--font-family)`, font-size `var(--text-body)`, line-height 1.5
- All headings, buttons, inputs inherit font-family

**Section 3 — Component Styles**
Implement styles for each UI-SPEC component:

1. **Skip link**: `.skip-link` — visually hidden, positioned absolute top 8px left 8px, appears on `:focus` with z-index 1000, background `var(--color-primary)`, color `var(--color-on-brand)`, padding 8px 16px, border-radius `var(--radius-md)`

2. **Header** (`.header`): background `var(--color-primary)`, height 56px, full width. Inner `.header__content` is max-width 1120px, centered, flex row, align-items center, padding 0 `var(--space-lg)`. Logo img 32px height. Product name in `.header__title` uses `var(--text-label)` size, `var(--weight-semibold)`, color `var(--color-on-brand)`, margin-left `var(--space-sm)`

3. **Main content** (`.main`): max-width 1120px, margin 0 auto, padding `var(--space-xl)` `var(--space-lg)`

4. **Section heading** (`.section-heading`): font-size `var(--text-heading)` (20px), font-weight `var(--weight-semibold)`, color `var(--color-text)`, margin 0 0 `var(--space-lg)` 0

5. **Provider cards container** (`.providers`): CSS Grid, `grid-template-columns: repeat(3, 1fr)`, gap `var(--space-lg)`

6. **Provider card** (`.provider-card`): white background, border 1px solid `var(--color-border)`, border-radius `var(--radius-lg)` (12px), padding `var(--space-lg)`, min-height 120px, cursor pointer, transition `border-color 150ms ease, box-shadow 150ms ease`, box-shadow `var(--shadow-card)`. No default button styling (reset background, border inherits from card, font inherits, text-align left, width 100%, display block). Each card gets a 4px top border via modifier classes:
   - `.provider-card--aws`: border-top 4px solid `var(--color-aws)`
   - `.provider-card--azure`: border-top 4px solid `var(--color-azure)`
   - `.provider-card--gcp`: border-top 4px solid `var(--color-gcp)`
   Hover state: border-color `var(--color-border-hover)`, box-shadow `var(--shadow-card-hover)`
   Focus state: outline 3px solid `var(--color-ring)`, outline-offset 2px

   Card inner elements: `.provider-card__name` in `var(--text-label)` size, `var(--weight-semibold)`, color `var(--color-text)`, margin-bottom `var(--space-xs)`, display block. `.provider-card__desc` in `var(--text-body)` size, color `var(--color-text-secondary)`, display block

7. **Workspace panel** (`.workspace`): background `var(--color-surface)`, border 1px solid `var(--color-border)`, border-radius `var(--radius-lg)`, min-height 200px, padding `var(--space-lg)`, margin-top `var(--space-xl)`. `.workspace__empty` text in `var(--color-text-secondary)`, text-align center, padding `var(--space-2xl)` 0

8. **Output panel** (`.output`): background `var(--color-output-bg)`, border-radius `var(--radius-lg)`, min-height 300px, margin-top `var(--space-xl)`, overflow hidden.

   Tab bar (`.output__tabs`): background `var(--color-output-surface)`, display flex, height 44px, align-items stretch. Tab buttons (`.output__tab`): background transparent, border none, padding 0 `var(--space-md)`, color `var(--color-output-muted)`, font-size `var(--text-label)`, font-weight `var(--weight-semibold)`, cursor pointer, border-bottom 2px solid transparent, height 100%, transition color 150ms ease, font-family inherit. Active tab (`.output__tab--active`): color `var(--color-output-text)`, border-bottom-color `var(--color-output-tab-active)`. Tab focus: outline 3px solid `var(--color-ring)`, outline-offset -3px (inset for dark bg)

   Toolbar (`.output__toolbar`): display flex, align-items center, padding `var(--space-sm)` `var(--space-md)`, border-bottom 1px solid `var(--color-output-border)`. Badge (`.output__badge`): background `var(--color-output-surface)`, color `var(--color-output-muted)`, font-size `var(--text-label)`, padding 4px 12px, border-radius 12px, margin-right auto. Action buttons (`.output__action`): background transparent, color `var(--color-output-text)`, border 1px solid `var(--color-output-border)`, border-radius `var(--radius-md)`, padding 8px 16px, font-size `var(--text-label)`, font-weight `var(--weight-semibold)`, font-family inherit, cursor pointer, margin-left `var(--space-sm)`, transition background 150ms ease. Hover: background `var(--color-output-surface)`. Focus: outline 3px solid `var(--color-ring)`, outline-offset 2px

   Content (`.output__content`): padding `var(--space-md)`. Tab panel (`.output__panel`): color `var(--color-output-muted)`

9. **Footer** (`.footer`): text-align center, padding `var(--space-md)`, font-size `var(--text-label)`, color `var(--color-text-secondary)`

10. **Responsive breakpoint**: `@media (max-width: 899px)` — `.providers` switches to `grid-template-columns: 1fr`. `.main` padding changes to `var(--space-lg)` `var(--space-md)`. `.header__title` gets `overflow: hidden`, `text-overflow: ellipsis`, `white-space: nowrap`
  </action>
  <verify>
    <automated>test -f css/styles.css && grep -q "\-\-color-primary: #002b49" css/styles.css && grep -q "\-\-color-aws: #ff9900" css/styles.css && grep -q "\-\-color-output-bg: #0a1628" css/styles.css && grep -q "max-width: 899px" css/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - css/styles.css exists and is non-empty
    - Contains `--color-primary: #002b49` (Infoblox Navy)
    - Contains `--color-accent: #f37021` (Infoblox Orange)
    - Contains `--color-secondary: #3a8fd6` (Infoblox Blue)
    - Contains `--color-surface-alt: #f5f7fa` (page background)
    - Contains `--color-aws: #ff9900`
    - Contains `--color-azure: #0078d4`
    - Contains `--color-gcp: #4285f4`
    - Contains `--color-output-bg: #0a1628` (Infoblox Dark)
    - Contains `--color-output-tab-active: #f37021` (orange tab indicator)
    - Contains `--font-family:` with system font stack including `-apple-system`
    - Contains `--radius-lg: 12px` (Token Calculator rounded-xl)
    - Contains `grid-template-columns: repeat(3, 1fr)` for provider cards
    - Contains `@media (max-width: 899px)` responsive breakpoint
    - Contains `.provider-card--aws` with `--color-aws`
    - Contains `.output__tab--active` with `--color-output-tab-active`
    - Contains `outline` with `--color-ring` for focus states
    - Contains `.skip-link` styles
  </acceptance_criteria>
  <done>Complete CSS file with all design tokens from UI-SPEC, component styles for header/cards/workspace/output/footer, and responsive breakpoint at 899px</done>
</task>

<task type="auto">
  <name>Task 2: Create HTML shell and SVG logo</name>
  <files>index.html, assets/infoblox-logo.svg</files>
  <read_first>
    .planning/phases/01-html-shell-branding/01-UI-SPEC.md
    css/styles.css
  </read_first>
  <action>
**Create `assets/infoblox-logo.svg`:**
White SVG wordmark reading "INFOBLOX" — simple text-based SVG. Use a bold sans-serif font, all caps. ViewBox approximately 160x32. Fill `#ffffff`. This is a placeholder; a real logo replaces it later. Example structure:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 160 32" fill="#ffffff">
  <text x="0" y="24" font-family="-apple-system, BlinkMacSystemFont, sans-serif" font-size="24" font-weight="700">INFOBLOX</text>
</svg>
```

**Create `index.html`:**
Full semantic HTML document with these exact elements (all copy from UI-SPEC Copywriting Contract):

1. `<!DOCTYPE html>`, `<html lang="en">`
2. `<head>`: charset utf-8, viewport meta (width=device-width, initial-scale=1), `<title>UDDI Permission Scope Helper - Infoblox</title>`, meta description "Generate least-privilege IAM policies for Infoblox Universal DDI cloud integrations with AWS, Azure, and GCP.", link to `css/styles.css`
3. `<body>`:

   a. **Skip link**: `<a href="#main-content" class="skip-link">Skip to main content</a>`

   b. **Header** (`<header class="header">`):
      - `<div class="header__content">`
      - `<img src="assets/infoblox-logo.svg" alt="Infoblox" class="header__logo" width="160" height="32">`
      - `<span class="header__title">UDDI Permission Scope Helper</span>`

   c. **Main** (`<main class="main" id="main-content">`):

      **Provider section** (`<section aria-label="Cloud provider selection">`):
      - `<h1 class="section-heading">Select Cloud Provider</h1>` (styled as `--text-heading`, 20px, visually not display-sized since "UDDI Permission Scope Helper" in header serves as page title)
      - `<nav class="providers" aria-label="Cloud provider selection">` containing three `<button>` elements:
        - `<button class="provider-card provider-card--aws" type="button" aria-label="Select Amazon Web Services (AWS)">` with `<span class="provider-card__name">Amazon Web Services (AWS)</span>` and `<span class="provider-card__desc">IAM policies for VPC discovery, DNS, and cloud forwarding</span>`
        - Same pattern for Azure: class `provider-card--azure`, aria-label "Select Microsoft Azure", name "Microsoft Azure", desc "Role assignments for network discovery, DNS, and cloud forwarding"
        - Same pattern for GCP: class `provider-card--gcp`, aria-label "Select Google Cloud Platform (GCP)", name "Google Cloud Platform (GCP)", desc "IAM roles for asset discovery, DNS, and cloud forwarding"

      **Workspace section** (`<section class="workspace" aria-label="Feature selection workspace">`):
      - `<p class="workspace__empty">Select a cloud provider above to begin.</p>`

      **Output section** (`<section class="output" aria-label="Policy output">`):
      - Tab bar: `<div class="output__tabs" role="tablist" aria-label="Output format">` with three tab buttons:
        - `<button class="output__tab output__tab--active" role="tab" id="tab-policy" aria-selected="true" aria-controls="panel-policy">Policy</button>`
        - `<button class="output__tab" role="tab" id="tab-terraform" aria-selected="false" aria-controls="panel-terraform">Terraform</button>`
        - `<button class="output__tab" role="tab" id="tab-guide" aria-selected="false" aria-controls="panel-guide">Setup Guide</button>`
      - Toolbar: `<div class="output__toolbar">` with:
        - `<span class="output__badge">0 permissions</span>`
        - `<button class="output__action" type="button">Copy</button>`
        - `<button class="output__action" type="button">Download</button>`
      - Content: `<div class="output__content">` with three tab panels:
        - `<div class="output__panel" role="tabpanel" id="panel-policy" aria-labelledby="tab-policy">Select a cloud provider and features to generate your permission policy.</div>`
        - `<div class="output__panel" role="tabpanel" id="panel-terraform" aria-labelledby="tab-terraform" hidden></div>`
        - `<div class="output__panel" role="tabpanel" id="panel-guide" aria-labelledby="tab-guide" hidden></div>`

   d. **Footer** (`<footer class="footer">`):
      - `<p>Based on Infoblox Universal DDI documentation. Not an official Infoblox product.</p>`
  </action>
  <verify>
    <automated>test -f index.html && test -f assets/infoblox-logo.svg && grep -q 'lang="en"' index.html && grep -q "UDDI Permission Scope Helper" index.html && grep -q 'role="tablist"' index.html && grep -q 'provider-card--aws' index.html && grep -q 'provider-card--azure' index.html && grep -q 'provider-card--gcp' index.html && grep -q "infoblox-logo.svg" index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html exists with `<html lang="en">`
    - Contains `<title>UDDI Permission Scope Helper - Infoblox</title>`
    - Contains meta description with "Generate least-privilege IAM policies"
    - Contains `href="css/styles.css"` stylesheet link
    - Contains skip-link anchor with href="#main-content"
    - Contains `<header class="header">` with infoblox-logo.svg reference
    - Contains `UDDI Permission Scope Helper` in header__title span
    - Contains `<main class="main" id="main-content">`
    - Contains `<nav class="providers" aria-label="Cloud provider selection">`
    - Contains three button elements with classes `provider-card--aws`, `provider-card--azure`, `provider-card--gcp`
    - Each provider button has `aria-label="Select ..."` with full provider name
    - AWS card contains text "Amazon Web Services (AWS)"
    - Azure card contains text "Microsoft Azure"
    - GCP card contains text "Google Cloud Platform (GCP)"
    - Contains workspace section with empty state text "Select a cloud provider above to begin."
    - Contains `role="tablist"` with three `role="tab"` buttons for Policy, Terraform, Setup Guide
    - First tab has `aria-selected="true"` and class `output__tab--active`
    - Contains `role="tabpanel"` elements with `aria-labelledby` referencing tab IDs
    - Contains badge with text "0 permissions"
    - Contains Copy and Download action buttons
    - Contains footer with "Based on Infoblox Universal DDI documentation. Not an official Infoblox product."
    - assets/infoblox-logo.svg exists and contains `<svg` and "INFOBLOX"
  </acceptance_criteria>
  <done>Complete semantic HTML shell with all components from UI-SPEC, SVG placeholder logo, correct ARIA attributes, and all copywriting contract text</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of HTML shell</name>
  <files>index.html</files>
  <what-built>Complete Infoblox-branded HTML/CSS shell with header, provider cards, workspace placeholder, dark output panel with tabs, and responsive layout.</what-built>
  <action>
Present the page to the user for visual verification. Open `index.html` in the default browser or instruct the user to open it. No code changes in this task — verification only.
  </action>
  <how-to-verify>
    1. Open `index.html` in a browser (file:// protocol works — no server needed)
    2. Verify header: Navy (#002b49) background, white "INFOBLOX" logo on left, "UDDI Permission Scope Helper" text in white
    3. Verify provider cards: Three cards in a row with colored top borders — AWS orange, Azure blue, GCP blue
    4. Verify hover: Mouse over a card — border darkens, subtle shadow appears
    5. Verify keyboard: Tab through all interactive elements — each shows a blue focus ring
    6. Verify workspace: White panel below cards showing "Select a cloud provider above to begin."
    7. Verify output panel: Dark navy background, tab bar with "Policy" active (orange underline), "Terraform" and "Setup Guide" dimmed, badge showing "0 permissions", Copy and Download buttons
    8. Verify responsive: Resize browser below 900px wide — cards stack vertically, layout remains usable
    9. Verify footer: Centered disclaimer text at bottom
  </how-to-verify>
  <verify>User confirms visual appearance matches UI-SPEC</verify>
  <done>User approves the visual rendering of all Phase 1 components</done>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Open index.html in browser — full layout renders with Infoblox branding
- Provider cards show correct brand colors (AWS orange, Azure blue, GCP blue)
- Output panel dark theme renders correctly
- Resize to < 900px — layout stacks vertically
- Tab through page — all interactive elements show focus rings
</verification>

<success_criteria>
- Three files exist: index.html, css/styles.css, assets/infoblox-logo.svg
- Page renders Infoblox Navy header with white logo and product name
- Three provider cards display with correct brand color top borders
- Dark output panel shows tab bar, action buttons, and badge placeholder
- Layout is responsive — stacks at < 900px
- All ARIA attributes present for accessibility
- All copywriting matches UI-SPEC contract exactly
</success_criteria>

<output>
After completion, create `.planning/phases/01-html-shell-branding/01-01-SUMMARY.md`
</output>
