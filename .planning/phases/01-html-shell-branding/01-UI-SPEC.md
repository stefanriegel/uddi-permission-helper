---
phase: 1
slug: html-shell-branding
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-16
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for the HTML shell and branding phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (vanilla HTML/CSS/JS) |
| Icon library | none (inline SVG for logo; CSS-only for UI chrome) |
| Font | system font stack: `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif` |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | CSS Custom Property | Value | Usage |
|-------|---------------------|-------|-------|
| xs | `--space-xs` | 4px | Icon gaps, inline padding |
| sm | `--space-sm` | 8px | Compact element spacing, button internal padding |
| md | `--space-md` | 16px | Default element spacing, card padding |
| lg | `--space-lg` | 24px | Section padding, card gaps |
| xl | `--space-xl` | 32px | Layout gaps between major panels |
| 2xl | `--space-2xl` | 48px | Major section breaks |
| 3xl | `--space-3xl` | 64px | Page-level vertical spacing |

Exceptions: Touch target minimum 44px for all interactive elements (provider cards, tab buttons, action buttons).

---

## Typography

| Role | CSS Custom Property | Size | Weight | Line Height |
|------|---------------------|------|--------|-------------|
| Body | `--text-body` | 16px | 400 (regular) | 1.5 |
| Label | `--text-label` | 14px | 600 (semibold) | 1.4 |
| Heading | `--text-heading` | 20px | 600 (semibold) | 1.2 |
| Display | `--text-display` | 28px | 600 (semibold) | 1.2 |

Display is used for the page title ("UDDI Permission Scope Helper"). Heading is used for section headers ("Select Cloud Provider", "Output"). Label is used for tab labels, button text, badge text, and card titles. Body is used for descriptions and instructional text.

---

## Color

### Brand Palette

| Role | CSS Custom Property | Value | Usage |
|------|---------------------|-------|-------|
| Dominant (60%) | `--color-surface` | `#ffffff` | Page background, main content area |
| Secondary (30%) | `--color-surface-alt` | `#f5f7fa` | Card backgrounds, section backgrounds |
| Accent (10%) | `--color-accent` | `#0058a2` | Header background, primary CTA buttons, active tab indicator, selected card border |
| Destructive | `--color-destructive` | `#d32f2f` | Destructive actions only (none in Phase 1) |

### Infoblox Branding

| Token | CSS Custom Property | Value | Usage |
|-------|---------------------|-------|-------|
| Infoblox Blue | `--color-infoblox` | `#0058a2` | Header bar, logo area, active states |
| Infoblox Blue Hover | `--color-infoblox-hover` | `#004a8a` | Header interactive hover state |
| Text on Brand | `--color-on-brand` | `#ffffff` | Text rendered on Infoblox Blue surfaces |

### Provider Brand Colors

| Provider | CSS Custom Property | Value | Usage |
|----------|---------------------|-------|-------|
| AWS | `--color-aws` | `#ff9900` | AWS card top border (4px), AWS card icon accent |
| Azure | `--color-azure` | `#0078d4` | Azure card top border (4px), Azure card icon accent |
| GCP | `--color-gcp` | `#4285f4` | GCP card top border (4px), GCP card icon accent |

### Output Panel (Dark Theme)

| Token | CSS Custom Property | Value | Usage |
|-------|---------------------|-------|-------|
| Panel BG | `--color-output-bg` | `#1e1e2e` | Output panel background |
| Panel Surface | `--color-output-surface` | `#2a2a3c` | Tab bar background, code block background |
| Panel Text | `--color-output-text` | `#e0e0e0` | Default text in output panel |
| Panel Muted | `--color-output-muted` | `#888899` | Placeholder text, disabled tab labels |
| Panel Border | `--color-output-border` | `#3a3a4c` | Dividers inside output panel |
| Tab Active | `--color-output-tab-active` | `#0058a2` | Active tab underline (2px) |

### Neutral Palette

| Token | CSS Custom Property | Value | Usage |
|-------|---------------------|-------|-------|
| Text Primary | `--color-text` | `#1a1a2e` | Body text, headings |
| Text Secondary | `--color-text-secondary` | `#555566` | Descriptions, help text |
| Border | `--color-border` | `#d0d5dd` | Card borders, dividers |
| Border Hover | `--color-border-hover` | `#0058a2` | Card hover/focus border |

Accent reserved for: header background, active tab indicator, selected provider card border, primary action buttons (Copy, Download), badge background.

---

## Component Inventory (Phase 1)

These are the HTML/CSS components delivered in this phase. All are static chrome -- no JS behavior yet.

### 1. Header Bar

- Full-width bar, background `--color-infoblox`, height 56px
- Left-aligned: Infoblox logo (SVG, 32px height, white) + product name "UDDI Permission Scope Helper" in `--text-label` at `#ffffff`
- No navigation links in Phase 1

### 2. Provider Card (x3)

- Arranged in a horizontal row with `--space-lg` (24px) gap
- Each card: white background (`#ffffff`), 1px border `--color-border`, border-radius 8px
- Top border: 4px solid in provider color (`--color-aws` / `--color-azure` / `--color-gcp`)
- Card padding: `--space-md` (16px)
- Content: provider name in `--text-label`, brief description in `--text-body` at `--color-text-secondary`
- Provider names: "Amazon Web Services (AWS)", "Microsoft Azure", "Google Cloud Platform (GCP)"
- Hover state: border transitions to `--color-border-hover`, box-shadow `0 2px 8px rgba(0, 0, 0, 0.1)`
- Cards are `<button>` elements for keyboard accessibility
- Min-height: 120px; equal-height via CSS Grid or Flexbox

### 3. Workspace Panel (placeholder)

- Below provider cards, full width
- Visible but displays empty state (see Copywriting Contract)
- Light background `--color-surface-alt`, border-radius 8px, border 1px `--color-border`
- Min-height: 200px
- Padding: `--space-lg` (24px)

### 4. Output Panel

- Below workspace panel, full width
- Dark theme: background `--color-output-bg`, border-radius 8px
- Min-height: 300px
- **Tab bar**: horizontal row at top, background `--color-output-surface`
  - Three tabs: "Policy", "Terraform", "Setup Guide"
  - Tab labels in `--text-label` at `--color-output-muted`
  - First tab ("Policy") shows active state: text `--color-output-text`, 2px bottom border `--color-output-tab-active`
  - Tab bar height: 44px (meets touch target)
- **Action buttons row**: right-aligned below tab bar
  - "Copy" button and "Download" button, both in `--text-label`
  - Button style: ghost on dark -- transparent background, `--color-output-text` text, 1px border `--color-output-border`, border-radius 4px, padding 8px 16px
  - Hover: background `--color-output-surface`
- **Badge placeholder**: left-aligned next to action buttons
  - Shows "0 permissions" in `--text-label` at `--color-output-muted`
  - Pill shape: background `--color-output-surface`, border-radius 12px, padding 4px 12px
- **Content area**: padding `--space-md` (16px)
  - Shows placeholder text in `--color-output-muted` (see Copywriting Contract)

### 5. Footer

- Full width, padding `--space-md` (16px) vertical
- Text in `--text-body` size 14px at `--color-text-secondary`, centered
- Copy: "Based on Infoblox Universal DDI documentation. Not an official Infoblox product."

---

## Layout

### Desktop (viewport >= 900px)

```
+--[ Header Bar (full width, 56px) ]------------------------------+
|  [Logo] UDDI Permission Scope Helper                             |
+------------------------------------------------------------------+
|                                                                    |
|  +--[AWS Card]--+  +--[Azure Card]--+  +--[GCP Card]--+          |
|  |              |  |                |  |              |           |
|  +--------------+  +----------------+  +--------------+           |
|                                                                    |
|  +--[Workspace Panel (placeholder)]-----------------------------+ |
|  |  Select a cloud provider above to begin                      | |
|  +--------------------------------------------------------------+ |
|                                                                    |
|  +--[Output Panel (dark)]---------------------------------------+ |
|  |  [Policy] [Terraform] [Setup Guide]    [0 perms] [Copy][DL] | |
|  |  ---------------------------------------------------------- | |
|  |  Select a cloud provider and features to generate output     | |
|  +--------------------------------------------------------------+ |
|                                                                    |
|  +--[Footer]----------------------------------------------------+ |
+------------------------------------------------------------------+
```

- Max content width: 1120px, centered with auto margins
- Side padding: `--space-lg` (24px)
- Provider cards: CSS Grid `grid-template-columns: repeat(3, 1fr)` with `--space-lg` gap

### Mobile / Tablet (viewport < 900px)

- Provider cards stack vertically: `grid-template-columns: 1fr`
- All panels full width with `--space-md` (16px) side padding
- Header height remains 56px, product name may truncate with ellipsis
- Output panel tab labels use abbreviated text if needed (no abbreviation required at 900px -- all three labels fit)

### Breakpoint

| Breakpoint | Value | Behavior |
|------------|-------|----------|
| Stack | `max-width: 899px` | Cards and panels stack vertically |
| Side-by-side | `min-width: 900px` | Cards in 3-column grid, panels full width |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Page title (in header) | UDDI Permission Scope Helper |
| Page `<title>` | UDDI Permission Scope Helper - Infoblox |
| Page meta description | Generate least-privilege IAM policies for Infoblox Universal DDI cloud integrations with AWS, Azure, and GCP. |
| AWS card title | Amazon Web Services (AWS) |
| AWS card description | IAM policies for VPC discovery, DNS, and cloud forwarding |
| Azure card title | Microsoft Azure |
| Azure card description | Role assignments for network discovery, DNS, and cloud forwarding |
| GCP card title | Google Cloud Platform (GCP) |
| GCP card description | IAM roles for asset discovery, DNS, and cloud forwarding |
| Workspace empty state | Select a cloud provider above to begin. |
| Output empty state | Select a cloud provider and features to generate your permission policy. |
| Output tab 1 | Policy |
| Output tab 2 | Terraform |
| Output tab 3 | Setup Guide |
| Badge placeholder | 0 permissions |
| Copy button | Copy |
| Download button | Download |
| Footer | Based on Infoblox Universal DDI documentation. Not an official Infoblox product. |

No destructive actions exist in Phase 1. No error states exist in Phase 1 (static HTML shell only).

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|----------------|
| Provider cards | `<button>` elements with `aria-label="Select {provider name}"` |
| Tab bar | `role="tablist"` with `role="tab"` on each tab, `aria-selected` on active tab |
| Tab panel | `role="tabpanel"` with `aria-labelledby` referencing the active tab |
| Color contrast | All text meets WCAG 2.1 AA (4.5:1 for body, 3:1 for large text) |
| Focus indicators | 2px outline `--color-infoblox` with 2px offset on all interactive elements |
| Skip link | Hidden "Skip to main content" link visible on focus, targets main content area |
| Landmark regions | `<header>`, `<main>`, `<footer>` semantic elements |
| Language | `<html lang="en">` |

---

## CSS Architecture

All styles in a single `styles.css` file using CSS custom properties for tokens.

```
:root {
  /* Spacing */
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;
  --space-3xl: 64px;

  /* Typography */
  --font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  --text-body: 16px;
  --text-label: 14px;
  --text-heading: 20px;
  --text-display: 28px;
  --weight-regular: 400;
  --weight-semibold: 600;

  /* Colors (all tokens listed in Color section above) */
}
```

File structure for Phase 1:
- `index.html` -- semantic HTML shell
- `css/styles.css` -- all custom properties and component styles
- `assets/infoblox-logo.svg` -- Infoblox logo (white, for header)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | n/a | not applicable |

No component registries. Pure vanilla HTML/CSS. External dependencies limited to Prism.js CDN (loaded in later phases for syntax highlighting, not in Phase 1).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
